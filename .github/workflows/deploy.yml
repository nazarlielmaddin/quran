name: Deploy to GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          EXPORT: "1"
          NEXT_PUBLIC_BASE_PATH: "/saadat"
      - name: Debug - Check output directory
        run: |
          echo "Contents of out directory:"
          ls -lah out/ || echo "out directory not found"
          echo "Tree of out (first 20 files):"
          find out -type f | head -20 || echo "no files in out"
        shell: bash
      - name: Deploy to GitHub Pages
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git checkout --orphan gh-pages
          git rm -r --cached .
          cp -r out/* .
          rm -rf out .next node_modules
          echo '!.next' >> .gitignore
          git add -A
          git commit -m "Deploy to GitHub Pages" || true
          git push -f origin gh-pages
        shell: bash